services:
  - type: web
    name: pastureai
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    nodeVersion: 20
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
